name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    # Apple silicon runner; the apple/container dependency needs Swift 6.2+ (Xcode 26)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select latest Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Swift version
        run: swift --version

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('Package.resolved') }}
          restore-keys: spm-${{ runner.os }}-

      - name: Build app bundle
        run: |
          export VERSION="${GITHUB_REF_NAME#v}"
          scripts/bundle.sh

      - name: Zip app
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          ditto -c -k --keepParent "dist/Davit.app" "Davit-${VERSION}.zip"
          shasum -a 256 "Davit-${VERSION}.zip" > "Davit-${VERSION}.zip.sha256"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Davit-*.zip
            Davit-*.zip.sha256
          generate_release_notes: true
          body: |
            ## Install

            1. Download `Davit-${{ github.ref_name }}.zip` below and unzip.
            2. Move `Davit.app` to /Applications.
            3. This build is not notarized (ad-hoc signature), so macOS will refuse to
               open it until you clear the quarantine flag:

               ```sh
               xattr -dr com.apple.quarantine /Applications/Davit.app
               ```

            Requires an Apple silicon Mac. On first launch Davit offers to install
            Apple's container platform if it isn't present (no admin rights needed).
